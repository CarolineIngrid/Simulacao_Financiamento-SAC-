function simulacao() {
  // variaveis
  var valor = parseFloat(valor);
  var prazoAnos = parseInt(prazoAnos);
  var jurosAno = parseFloat(jurosAno);

  //pegando dados usuario
  var valor = parseFloat(document.getElementById("inputvalor").value);
  var prazoAnos = parseInt(document.getElementById("inputprazoAnos").value);
  var jurosAno = parseFloat(document.getElementById("inputjurosAno").value);

  //cálculo prazo meses
  var resultadoprazoMeses = prazoAnos * 12;

  // cálculo juros ao mês
  var resultadojurosMes = (1 + jurosAno) ** (1 / 12) - 1;

  // //cálculo juros
  // var jurosTotal = (valor - +(prestação * amortização)) * resultadojurosMes;
  // console.log(
  //   (jurosTotal = (valor - +(prestação * amortização)) * resultadojurosMes)
  // );
  //amortização

  var amortização = [];
  var prestação = [];
  var jurosTotal = [];
  var saldoDevedor = [];
  var jurosAcumulado = [];
  for (var contador = 0; contador <= 5; contador++) {
    amortização.push(valor / resultadoprazoMeses);
    saldoDevedor.push(valor - contador * amortização[contador]);
    jurosTotal.push(saldoDevedor[contador] * resultadojurosMes);
    prestação.push(contador);
    jurosAcumulado.push(jurosTotal[contador]++);
  }

  console.log(amortização);
  console.log(saldoDevedor);
  console.log(jurosTotal);
  console.log(prestação);
  console.log(jurosAcumulado);

  // imprimindo os resultados
  document.getElementById("inputresultadojurosMes").value = resultadojurosMes;
  document.getElementById("inputresultadoprazoMeses").value =
    resultadoprazoMeses;

  // //prestações

  // while (prestação <= 5) {
  //   console.log(prestação);
  //   prestação++;
  //   if (prestação == 5) {
  //     break;
  //   }
  // }
  // console.log(prestação);

  //loggggg
  console.log(valor);
  console.log(resultadoprazoMeses);
  console.log(resultadojurosMes);

  var tabela = Array();

  tabela["Prestação"] = Array();
  tabela["Amortização"] = Array();
  tabela["Juros"] = Array();
  tabela["Total"] = Array();

  tabela["prestação"][1] = prestação;

  tabela["Amortização"][1] = amortização;
  tabela["Amortização"][2] = amortização;
  tabela["Amortização"][3] = amortização;
  tabela["Amortização"][4] = amortização;

  tabela["Juros"][1] = document.getElementById(
    "resultadoAmortizacao"
  ).innerHTML = tabela["Amortização"][2];
}
